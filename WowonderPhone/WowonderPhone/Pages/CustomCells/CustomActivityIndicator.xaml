<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WowonderPhone.Controls;assembly=WowonderPhone"
             x:Class="WowonderPhone.Pages.CustomCells.CustomActivityIndicator">

    <ContentView.Content>
        <Grid
            x:Name="Wrapper">

            <BoxView
                HeightRequest="64" 
                WidthRequest="64" 
                BackgroundColor="{DynamicResource AccentColor}" />

            <Label
                Text="{ x:Static local:WowonderShapesFont.LogoGrial }"
                Style="{ DynamicResource WowondershapesFontIcon }"
                TextColor="White"
                FontSize="45"
                VerticalTextAlignment="Center"
                HorizontalTextAlignment="Center"/>
        </Grid>
    </ContentView.Content>
</ContentView>
    